$(document).ready(function() {
  var inputElements = $('input');        // Alle input-Elemente
  var max = $('#max');                   // Element mit ID "max"
  var oddElements = $('.odd');           // Elemente mit Klasse "odd"
  var elements = $('td, th');            // Alle td- und tr-Elemente
});

$(document).ready(function() {
  var inputMax = $('input[name="max"]'); // input-Elemente, deren
                                         // name-Attribut den Wert "max" hat
});